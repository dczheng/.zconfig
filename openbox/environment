#
# Set system-wide environment variables here for Openbox
# User-specific variables should be placed in $HOME/.config/openbox/environment
#

# To set your language for displaying messages and time/date formats, use the following:
#LANG=en_CA.UTF8

# To set your keyboard layout, you need to modify your X config:
# http://www.google.com/search?q=how+to+set+keyboard+layout+xorg
HOST=`hostname`
if [ $HOST = 'archlinux' ]
then
    export PATH=/home/dczheng/local/usr/mathematica_10.0/bin:$PATH
elif [ $HOST = 'debian' ]
then
    xrandr --output VGA-2 --mode 1920x1080 --pos 0x0 --rotate normal --output DVI-D-1 --primary --mode 1280x1024 --pos 1920x56 --rotate normal --output HDMI-1 --mode 1920x1080 --pos 3200x0 --rotate normal
else
    echo "Unupported host!"
fi
